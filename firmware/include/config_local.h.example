#pragma once

// Local WiFi credentials — copy this file to config_local.h and fill in your values.
// config_local.h is gitignored and will never be committed.

#define WIFI_SSID "your_network_name"
#define WIFI_PASS "your_password"
